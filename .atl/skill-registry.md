# Skill Registry — college

<!-- Auto-generated by gentle-pi extensions/skill-registry.ts. Run /skill-registry:refresh to regenerate. -->

Last updated: 2026-09-08

## Sources scanned

- /home/nachosag/.pi/agent/skills

## Contract

**Delegator use only.** This registry is an index, not a summary. Any agent that launches subagents reads it to select relevant skills, then passes exact `SKILL.md` paths for the subagent to read before work.

`SKILL.md` remains the source of truth. Do not inject generated summaries or compact rules by default; pass paths so subagents load the full runtime contract and preserve author intent.

## Skills

| Skill | Trigger / description | Scope | Path |
| --- | --- | --- | --- |
| `human-writing` | Trigger: human-like writing, natural text, remove AI fingerprint, avoid robotic tone, write like a person, humanize, this sounds like AI. Write text that sounds like a real human — with voice, opinions, imperfections, and rhythm. Erase common AI patterns and produce natural, authentic prose. | user | `/home/nachosag/.pi/agent/skills/human-writing/SKILL.md` |
| `learn` | Trigger: explain, teach, learn, understand, ELI5, walk me through, quiz me, flashcard, study guide, not getting it, confused, won't stick, what is X, prerequisites, explicame, no entiendo, quiero aprender. Tutoring and learning mode — help users understand concepts through guided discovery, diagnosis, and active recall. Use mermaid skill for diagrams when a visual helps. | user | `/home/nachosag/.pi/agent/skills/learn/SKILL.md` |
| `mermaid` | Trigger: diagram, flowchart, sequence, graph, mermaid, diagrama, visualize, architecture diagram. Generate Mermaid diagrams that render natively in Markdown — flowcharts, sequence diagrams, class diagrams, state diagrams, entity relationships, Gantt, pie, git graphs, mindmaps, timelines, user journeys, kanban, sankey, quadrant charts, block diagrams, C4, architecture, requirement diagrams, and more. | user | `/home/nachosag/.pi/agent/skills/mermaid/SKILL.md` |
| `pnpm` | Trigger: pnpm, package manager, Node.js project init, installing dependencies (global or project), install library, add package, monorepo, workspaces, node_modules, pnpm-lock. Load this when initializing a Node.js project, adding/removing packages, or managing dependencies — ALWAYS use pnpm, never npm or yarn. pnpm commands, workspace config, and best practices. | user | `/home/nachosag/.pi/agent/skills/pnpm/SKILL.md` |
| `source-of-truth` | Trigger: source of truth, source-of-truth, alimentar source-of-truth, documentar proyecto, project history, historial del proyecto. Scans a project and writes its section under source-of-truth/projects/. | user | `/home/nachosag/.pi/agent/skills/source-of-truth/SKILL.md` |
| `typescript` | TypeScript strict patterns and best practices. Trigger: When writing TypeScript code — types, interfaces, generics, strict configuration, or any .ts/.tsx implementation file. Always load for implementation work. Skip when reviewing pure config or test utilities unless types are being changed. | user | `/home/nachosag/.pi/agent/skills/typescript/SKILL.md` |
| `uv` | Trigger: uv, Python package manager, Python project init, installing dependencies (global or project), install library, add package, pip replacement, pyproject.toml, uv.lock, virtual environments. Load this when initializing a Python project, adding/removing packages, or managing dependencies — ALWAYS use uv, never pip. uv commands, workflows, and configuration. | user | `/home/nachosag/.pi/agent/skills/uv/SKILL.md` |

## Loading protocol

1. Match task context and target files against the `Trigger / description` column.
2. Pass only the matching `Path` values to the subagent under `## Skills to load before work`.
3. Instruct the subagent to read those exact `SKILL.md` files before reading, writing, reviewing, testing, or creating artifacts.
4. If no matching skill exists, proceed without project skill injection and report `skill_resolution: none`.
