# Propiedades del determinante

1. $\det(kM)=k^{n}\cdot \det(M)$, donde $k$ es un escalar y $n$ es el tamaño de la matriz $M$.
2. $\det(M\cdot N)=\det(M)\cdot \det(N)$.
3. $\det(M^{-1})=\frac{1}{\det(M)}$.
4. $\det(M^{n})=\left( \det(M) \right)^{n}$.