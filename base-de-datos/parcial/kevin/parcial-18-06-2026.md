# UNGS | Base de datos II

## Cassandra - Basado en el ejercicio de HOTELES desarrollado en clase:

### 1. Listar los comentarios de huéspedes sobre un hotel, filtrados por calificación (por ejemplo, solo los de 5 estrellas), ordenados por fecha.

```cql
CREATE TABLE comments_by_hotel_and_score (
	hotel_name TEXT,
	comment_title TEXT,
	comment_body TEXT,
	comment_score INT,
	comment_publication_date DATE,
	comment_id UUID,
	
	PRIMARY KEY (
		(hotel_name, comment_score), 
		comment_publication_date, 
		comment_id
	)
) WITH CLUSTERING ORDER BY (comment_publication_date DESC) ;

INSERT INTO comments_by_hotel_and_score (
	hotel_name,
	comment_title,
	comment_body,
	comment_score,
	comment_publication_date,
	comment_id
) VALUES (
	'Hilton',
	'Muy buena estadía',
	'Excelente',
	5,
	'2026-06-20',
	uuid('comment01')
) ;

INSERT INTO comments_by_hotel_and_score (
	hotel_name,
	comment_title,
	comment_body,
	comment_score,
	comment_publication_date,
	comment_id
) VALUES (
	'Hilton',
	'Muy buena estadía',
	'Excelente',
	5,
	'2026-06-20',
	uuid('comment02')
) ;

SELECT *
FROM comments_by_hotel_and_score
WHERE hotel_name = 'Hilton' 
	AND hotel_score = 5 ;

PK('Hilton', 'Muy buena estadía', 'Excelente', 5, '2026-06-20', uuid('comment01'))
PK('Hilton', 'Muy buena estadía', 'Excelente', 5, '2026-06-20', uuid('comment02'))
```

### 2. Consultar las reservas realizadas por un huésped dentro de un hotel específico, ordenadas por fecha de inicio.

```cql
CREATE TABLE reservations_by_guest_and_hotel (
	guest_id UUID,
	hotel_name TEXT,
	reservation_start_date DATE,
	reservation_end_date DATE,
	room_number INT,
	
	PRIMARY KEY (
		(guest_id, hotel_name),
		reservation_start_date,
		room_number
	)
) WITH CLUSTERING ORDER BY (reservation_start_date DESC) ;

INSERT INTO reservations_by_guest_and_hotel (
	guest_id,
	hotel_name,
	reservation_start_date,
	reservation_end_date,
	room_number,
) VALUES (
	uuid('abc001'),
	'Hilton',
	'2026-06-01',
	'2026-06-02',
	101,
) ;

INSERT INTO reservations_by_guest_and_hotel (
	guest_id,
	hotel_name,
	reservation_start_date,
	reservation_end_date,
	room_number,
) VALUES (
	uuid('abc001'),
	'Hilton',
	'2026-06-01',
	'2026-06-02',
	102,
) ;

SELECT *
FROM reservations_by_guest_and_hotel
WHERE guest_id = uuid('abc001') 
	AND hotel_name = 'Hilton' ;

PK(uuid('abc001'), 'Hilton', '2026-06-01', 101)
PK(uuid('abc001'), 'Hilton', '2026-06-01', 102)
```

### 3. Listar habitaciones disponibles de un determinado tipo (por ejemplo, "Suite") en un hotel y fecha concreta.

```cql
CREATE TABLE avilable_rooms_by_type_hotel_and_date (
	hotel_name TEXT,
	availability_date DATE,
	room_type TEXT,
	room_number INT,
	
	PRIMARY KEY (
		(hotel_name, availability_date, room_type), 
		room_number
	)
) ;

INSERT INTO avilable_rooms_by_type_hotel_and_date (
	hotel_name,
	availability_date,
	room_type,
	room_numer
) VALUES (
	'Hilton',
	'2026-06-11',
	'Suite',
	101
) ;

INSERT INTO avilable_rooms_by_type_hotel_and_date (
	hotel_name,
	availability_date,
	room_type,
	room_numer
) VALUES (
	'Hilton',
	'2026-06-11',
	'Suite',
	102
) ;

SELECT *
FROM available_rooms_by_type_hotel_and_date
WHERE hotel_name = 'Hilton' 
	AND available_date = '2026-06-11' 
	AND	room_type = 'Suite' ;

PK('Hilton', '2026-06-11', 'Suite', 101)
PK('Hilton', '2026-06-11', 'Suite', 102)
```

### 4. Listar todas las amenities de habitaciones que pertenecen a una categoría (por ejemplo, "Lujo", "Económica") en un hotel.

```cql
CREATE TABLE amenities_by_hotel_name_and_room_category (
	hotel_name TEXT,
	room_category TEXT,
	room_number INT,
	amenity_name TEXT,
	
	PRIMARY KEY (
		(hotel_name, room_category), 
		room_number,
		amenity_name
	)
) ;

INSERT INTO amenities_by_hotel_name_and_room_category (
	hotel_name,
	room_category,
	amenity_name
) VALUES (
	'Hilton',
	'Lujo',
	101,
	'Jacuzzi'
) ;

INSERT INTO amenities_by_hotel_name_and_room_category (
	hotel_name,
	room_category,
	amenity_name
) VALUES (
	'Hilton',
	'Lujo',
	101,
	'Smart TV'
) ;

INSERT INTO amenities_by_hotel_name_and_room_category (
	hotel_name,
	room_category,
	amenity_name
) VALUES (
	'Hilton',
	'Lujo',
	102,
	'Jacuzzi'
) ;

INSERT INTO amenities_by_hotel_name_and_room_category (
	hotel_name,
	room_category,
	amenity_name
) VALUES (
	'Hilton',
	'Lujo',
	102,
	'Minibar'
) ;

SELECT *
FROM amenities_by_hotel_name_and_room_category
WHERE hotel_name = 'Hilton'
	AND room_category = 'Lujo' ;

PK('Hilton', 'Lujo', 101, 'Smart TV')
PK('Hilton', 'Lujo', 101, 'Minibar')
PK('Hilton', 'Lujo', 102, 'Jacuzzi')
PK('Hilton', 'Lujo', 102, 'Minibar')
```

Para cada consulta se pide:

A. Diseñar la tabla, identificando todas las claves.
B. Instanciar de manera tal de mostrar la unicidad de la PK.
C. Realizar el CQL que permite resolver la consulta.

## Teoría

### 5. Realizar un diagrama de una base de datos relacional en cluster con 5 nodos. Indicar sus componentes y sus SPOF.

### 6. Realizar un diagrama de un cluster en MongoDB con 4 nodos, indicando la configuración recomendada por Mongo y los componentes principales.

### 7. 5 Diferencias entre MySQL y MongoDB analizando su diseño.

### 8. 5 Diferencias entre MongoDB y Cassandra analizando la arquitectura.

### 9. Explicar las taxonomías de NoSQL indicando un ejemplo en cada una (5 vistas en clase).

![[taxonomías-nosql]]